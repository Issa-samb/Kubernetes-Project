apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "checkoutservice.fullname" . }}
  labels:
    {{- include "checkoutservice.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      {{- include "checkoutservice.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "checkoutservice.selectorLabels" . | nindent 8 }}
    spec:
      imagePullSecrets:
        - name: gitlab-registry
      containers:
        - name: checkoutservice
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - containerPort: {{ .Values.service.port }}
          env:
            - name: PORT
              value: {{ .Values.env.PORT | quote }}
            - name: DISABLE_PROFILER
              value: {{ .Values.env.DISABLE_PROFILER | quote }}
            - name: PRODUCT_CATALOG_SERVICE_ADDR
              value: {{ .Values.env.PRODUCT_CATALOG_SERVICE_ADDR }}
            - name: CURRENCY_SERVICE_ADDR
              value: {{ .Values.env.CURRENCY_SERVICE_ADDR }}
            - name: CART_SERVICE_ADDR
              value: {{ .Values.env.CART_SERVICE_ADDR }}
            - name: SHIPPING_SERVICE_ADDR
              value: {{ .Values.env.SHIPPING_SERVICE_ADDR }}
            - name: EMAIL_SERVICE_ADDR
              value: {{ .Values.env.EMAIL_SERVICE_ADDR }}
            - name: PAYMENT_SERVICE_ADDR
              value: {{ .Values.env.PAYMENT_SERVICE_ADDR }}
          resources:
            {{- toYaml .Values.resources | nindent 12 }}
          readinessProbe:
            tcpSocket:
              port: {{ .Values.service.port }}
            initialDelaySeconds: 10
            periodSeconds: 10
          livenessProbe:
            tcpSocket:
              port: {{ .Values.service.port }}
            initialDelaySeconds: 20
            periodSeconds: 20
